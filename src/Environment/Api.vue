<template>
  <div class="flex-grow overflow-y-scroll overflow-x-hidden">
    <div class="py-6 mx-auto max-w-1xl">
      <p>🤷‍♂️🐯 We don't really have an api yet...working on it.</p>
      <!-- <p>For now if you just want to monkey with some things here is a thoguht:</p>
      <p>We use FaunaDB and we do have an api for authentication, so you can get yourself an
        access token and use it to make requests directly to fauna. Which is actually what 
        happens in the app.
      </p>
      <p>At some point we will have an api that wont require direct use of fauna.</p>
      <br>
      <code>
        curl -X POST -H "Content-Type: application/json" -d '{"username":"admin","password":"admin"}' https://shere.netlify.app/api/login 
      </code>
      <br>
      <br>
      <p>You'll get back your account meta information, an access token, and a refresh token</p>
      <p>Then you can call the following Fauna UDFs with the token:</p>
      <br>
      <ul>
        <li>
          <code>
            q.Call("GetWorkspacesByAccount")
          </code>
        </li>
        <li>
          <code>
            q.Call("GetWorkspace", workspaceId)
          </code>
        </li>
        <li>
          <code>
            q.Call("GetWorkspacesByAccount")
          </code>
        </li>
      </ul> -->
    </div>
  </div>
</template>